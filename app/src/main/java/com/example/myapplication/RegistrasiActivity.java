package com.example.myapplication;

import androidx.appcompat.app.AppCompatActivity;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;

import com.example.myapplication.util.PreferencesHelper;

public class RegistrasiActivity extends AppCompatActivity {


 //   private Button btnRegister;
  //  private EditText etUser, etPassword;
  //  PreferencesHelper preferencesHelper;

 //   @Override
 //   protected void onCreate(Bundle savedInstanceState) {
 //       super.onCreate(savedInstanceState);
  //      setContentView(R.layout.activity_registrasi);


   //     preferencesHelper = PreferencesHelper.getINSTANCE(getApplicationContext());

  //      btnRegister = findViewById(R.id.btnRegister);
   //     etUser = findViewById(R.id.etUser);
   //     etPassword = findViewById(R.id.etPassword);


    //    btnRegister.setOnClickListener(v -> {
      //      preferencesHelper.setLogin(true);
     //       preferencesHelper.setUser(etUser.getText().toString());
     //       preferencesHelper.setPassword(etPassword.getText().toString());
      //      Intent homeIntent = new Intent(RegistrasiActivity.this, FirstActivity.class);
      //      startActivity(homeIntent);
      //  });

  //  }
}